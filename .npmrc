# Expo SDK 57 + npm 12: strict peer resolution conflicts with the
# `react-native-windows` peerOptional in @react-native-community/datetimepicker,
# so we relax peer deps (standard, safe for Expo apps). Keeps `npm install`
# working without needing the --legacy-peer-deps flag each time.
legacy-peer-deps=true
